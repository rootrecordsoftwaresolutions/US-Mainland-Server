# Mainland system monitor

Minimal Pacific-inspired host monitor.

- CPU count/load
- memory
- root filesystem
- uptime/hostname/platform
- one current file only: `system-current.json`

No samples directory, archive, rename, database, or history.